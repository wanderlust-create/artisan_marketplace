<h1>Your Artisans</h1>

<div class="actions" style="margin-bottom: 1rem;">
  <%= link_to 'Add New Artisan' , new_admin_artisan_path(@admin), class: 'btn btn-primary' %>
</div>

<% if @artisans.any? %>
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Store Name</th>
        <th>Email</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% @artisans.each do |artisan| %>
        <tr>
          <td>
            <%= artisan.store_name %>
          </td>
          <td>
            <%= artisan.email %>
          </td>
          <td>
            <%= link_to "Show Artisan" , artisan_path(artisan.id), class: 'btn btn-secondary' %>
          </td>
        </tr>
        <% end %>
    </tbody>
  </table>
  <% else %>
    <p>You currently have no artisans.</p>
    <% end %>

      <%= link_to 'Back to Dashboard' , dashboard_admin_path(@admin), class: 'btn btn-primary' %>